import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container : {
        flex:1,
    },
    backButon : {
        position: 'absolute',
        top: 40,
        left: 20,
        padding: 10,
        borderRadius: 50,
        overflow: 'hidden',
        },
})